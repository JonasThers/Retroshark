import React from 'react';

const Case = (props) => {
    return (
        <div>
            <div>{props.title}</div>
            <div>{props.description}</div>
        </div>
    )
}

export default Case;